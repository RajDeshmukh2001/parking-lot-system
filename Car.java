public class Car extends Vehicle {
    public Car(String id, String registrationNumber) {
        setId(id);
        setRegistrationNumber(registrationNumber);
        setInTimeStamp(registrationNumber);
    }
}
